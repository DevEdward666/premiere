import {StyleSheet} from 'react-native'
const styles = StyleSheet.create({
    avatar: {
      width: 80,
      height: 80,
      borderRadius: 120,
      borderWidth: 1,
      borderColor: 'white',
      alignContent: 'center',
      marginBottom: 40,
    },
  });

  export default styles